# گزارش تغییرات کد اختصاصی bsma.ir

- **ریپو:** `peymandarakhshani111-afk/bsma`، شاخه‌ی `claude/keen-albattani-it7rx4`
- **مسیر روی هاست:** `wp-content/mu-plugins/`

## ۰. دامنه‌ی کار

**منبع کد:**
- ۱۰ فایل PHP که مستقیم داخل `wp-content/mu-plugins` بودند.
- صاحب سایت این فایل‌ها را از File Manager در cPanel در قالب zip دانلود کرد.
- فایل‌های `.bak`، `.zip`، `.json` و `.php0` در zip نبودند و بررسی نشدند.

**Commitها به ترتیب:**

| Commit | توضیح |
|---|---|
| `67d68eb` | کد اصلی سایت، بدون هیچ تغییر (مبنای مقایسه) |
| `4213e8c` | اصلاح‌های مرحله‌ی اول |
| `1899e1f` | اصلاح‌های بازبینی دوم |
| `951b2f4` | بهبود سرعت، و برگرداندن یک اشتباه خودم (بخش ۴) |
| commit آخر | همین گزارش، به‌علاوه‌ی اصلاح یک توضیح نادرست داخل کد (بخش ۴، مورد ۶) |

diff کامل همه‌ی تغییرات کد با این دستور دیده می‌شود:

```
git diff 67d68eb claude/keen-albattani-it7rx4 -- wp-content/
```

**فایل‌های تغییرکرده:**
- ۶ فایل ویرایش شد:
  - `fire-account-widget.php`
  - `post.php`
  - `digikala-product-archive.php`
  - `dk-single-product.php`
  - `dk-wishlist.php`
  - `cart-quantity-fix.php`
- ۱ فایل جدید اضافه شد: `bsma-performance.php`

**فایل‌های بدون تغییر:**
- `bsma-fix-pw-schema-price.php`
- `bsma-product-schema-tweaks.php`
- `ثلاث.php`
- `یع جع.php`

**روی سایت زنده هیچ کاری نکرده‌ام:** نه فایلی آپلود کرده‌ام، نه تنظیمی را تغییر داده‌ام. نصب فایل‌ها با صاحب سایت است (بخش ۶).

---

## ۱. روش تست و محدودیت‌های آن

### محیط تست

| مورد | مقدار |
|---|---|
| وردپرس | 6.8.2 (از مخزن رسمی WordPress در GitHub) |
| ووکامرس | 10.1.2 |
| PHP | 8.4 |
| دیتابیس | SQLite با افزونه‌ی رسمی `sqlite-database-integration` 2.2.3 (به جای MySQL) |
| قالب | `twentytwentyfive` |
| وب‌سرور | وب‌سرور داخلی PHP |

### داده‌ی تست

- **محصولات:** ۳۰ محصول ساده و ۱ محصول متغیر.
  - یک محصول ناموجود است.
  - محصولات شماره‌ی ۵، ۱۰، ۱۵، ۲۰، ۲۵ و ۳۰ بدون قیمت‌اند.
- **دسته‌ها:** دسته‌ی مادر با شناسه‌ی **279**، که کارت تبلیغاتی نوار کناری فقط برای آن فعال است، به همراه زیردسته‌ها با ترتیب دستی.
- **برند و ویژگی:** برندها، یک ویژگی سراسری (`pa_color`) و یک ویژگی سفارشی.
- **تصاویر:** ۱۲ تصویر JPEG ساخته‌شده با GD، به ابعاد 2560×1440، 1200×1600 و 1600×1600.
- **نوشته‌ها:** سه نوشته‌ی وبلاگ که یکی از آن‌ها بلند است.
- **صفحه‌ها:** سبد خرید کلاسیک با `[woocommerce_cart]` و صفحه‌ای با `[fire_account_widget]`.
- **کاربر:** یک مشتری که رمزش `'` و `\` دارد.

### تست‌هایی که اجرا شد

1. **تست سرتاسری در مرورگر:** ۵۴ تست با Playwright و Chromium روی همین وردپرس. همه قبول شدند.
2. **تست واحد جاوااسکریپت در مرورگر:** ۲۰ تست برای لایت‌باکس و اسکریپت سبد خرید، و ۶ تست برای بازگرداندن محصولات بعد از خطای فیلتر (در این تست‌ها `fetch` شبیه‌سازی شده است). همه قبول شدند.
3. **۶ تست تکمیلی:**
   - کپچا روی نوشته‌ای که با قالب معمولی و بدون شورت‌کد نمایش داده می‌شود
   - محصولی که گالری‌اش تصویر حذف‌شده دارد
4. **آزمایش‌های جداگانه:**
   - رفتار `@font-face` خراب در Chromium
   - خطای اکشن‌های AJAX در `ثلاث.php`
   - رفتار فیلد تعداد در سبد
5. **بررسی کد اصلی:** همه‌ی باگ‌هایی که در این گزارش «روی کد اصلی تأیید شد» نوشته شده‌اند، روی همین وردپرس با کد اصلی (`67d68eb`) بازتولید شدند.
6. **بررسی syntax:** با `php -l` و `node --check`.
7. **لاگ PHP:** در آخرین اجرای همه‌ی تست‌ها روی نسخه‌ی نهایی، هیچ Warning، Notice یا Fatal از فایل‌های mu-plugins ثبت نشد. Fatalهای `ثلاث.php` فقط در آزمایش جداگانه‌ای دیده شدند که آن اکشن‌ها را عمداً صدا زد (بخش ۵، مورد ۲).
8. **برچسب «از روی کد»:** هر جا در این گزارش «از روی کد» نوشته شده، آن مورد با خواندن کد نتیجه‌گیری شده و تست نشده است.

### محدودیت‌ها

- **دیتابیس:** SQLite به جای MySQL استفاده شد. پس `ALTER TABLE` که `dbDelta` روی جدول موجود MySQL اجرا می‌کند (بخش ۲.۵) تست نشده است.
- **قالب و CSS:** قالب BeTheme، Elementor و CSS سفارشی سایت در دسترس نبودند. CSS این کلاس‌ها بیرون از این فایل‌هاست و ظاهر واقعی‌شان در سایت دیده نشده است:
  - لایت‌باکس (`.dk2-lightbox-*`)
  - نوار چسبان
  - تصویر اصلی محصول
- **سایت زنده:** محیط من به bsma.ir دسترسی نداشت، چون سیاست شبکه‌ی این محیط اجازه نمی‌دهد. پس سایت زنده اندازه‌گیری نشد.
- **لاگ‌های غیرمرتبط:** خطاهای Action Scheduler ووکامرس به خاطر پشتیبانی نشدن `UPDATE ... JOIN` در SQLite بودند و به کد سایت مربوط نیستند.

---

## ۲. تغییرات فایل‌به‌فایل

شماره‌ی خط‌های بخش‌های ۲ تا ۵ مربوط به commit `9c9426a` هستند. تغییرات بخش ۷ خط‌های `dk-single-product.php` و `post.php` را بعد از آن جابه‌جا کرده‌اند.

### ۲.۱ `fire-account-widget.php` (شورت‌کد `[fire_account_widget]`)

| # | تغییر | خط | دلیل |
|---|---|---|---|
| 1 | تابع `fire_account_start_session` حذف شد. این تابع برای هر کاربر لاگین‌کرده، در هر درخواست `session_start()` را اجرا می‌کرد. پیام‌ها حالا در transient `fire_msg_{user_id}` با عمر ۵ دقیقه ذخیره می‌شوند. | 15 | session در PHP قفل دارد و درخواست‌های هم‌زمان یک کاربر را پشت سر هم اجرا می‌کند. اثر این قفل را اندازه نگرفته‌ام؛ رفتار عمومی PHP است. |
| 2 | به هر ۴ فرم `wp_nonce_field('fire_account_action', 'fire_nonce')` اضافه شد. | 201، 212، 241، 252 | جلوگیری از جعل درخواست (CSRF) |
| 3 | nonce در خط 32 بررسی می‌شود. اگر نامعتبر باشد، هیچ کاری انجام نمی‌شود و پیام «اعتبار فرم تمام شده است…» نمایش داده می‌شود. | 32 | CSRF |
| 4 | پردازشگر فرم فقط وقتی اجرا می‌شود که یکی از ۴ دکمه‌ی `fire_update_account`، `fire_change_password`، `fire_save_addresses` یا `fire_logout` ارسال شده باشد. | 23–28 | |
| 5 | برای تغییر رمز، فیلد جدید «رمز فعلی» (`password_current`) اضافه شد و با `wp_check_password` بررسی می‌شود. | 213، 63 | CSRF و امنیت حساب |
| 6 | به جای `wp_set_password` از `wp_update_user(['user_pass' => ...])` استفاده می‌شود. | 69 | در کد اصلی، بعد از تغییر رمز کاربر از حساب خارج می‌شد و پیام موفقیت را نمی‌دید (روی کد اصلی تأیید شد). `wp_update_user` لاگین را حفظ می‌کند (تست شد). |
| 7 | ایمیل با `is_email` بررسی می‌شود. اگر نامعتبر باشد، **هیچ‌کدام** از ایمیل و نام نمایشی ذخیره نمی‌شوند. اگر `wp_update_user` خطا بدهد (مثلاً ایمیل تکراری)، متن خطا نمایش داده می‌شود. | 43 | جزئیات زیر جدول |
| 8 | متن پیام با `esc_html` چاپ می‌شود. | 100 | |
| 9 | دو مورد `wp_redirect` به `wp_safe_redirect` تبدیل شد. | 71، 84 | |
| 10 | برای فیلدهای `$_POST` مقدار پیش‌فرض `?? ''` گذاشته شد. | | جلوگیری از هشدار PHP 8 |
| 11 | **`$_POST` عمداً slash‌دار باقی مانده است** (توضیح زیر جدول). | 37 | |

جزئیات ردیف ۷ (روی کد اصلی تأیید شد):
- ایمیل نامعتبر `not-an-email` به صورت رشته‌ی **خالی** ذخیره شد و پیام «اطلاعات حساب با موفقیت ذخیره شد» نمایش داده شد.
- ایمیل تکراری ذخیره نشد، ولی باز هم همان پیام موفقیت نمایش داده شد.

توضیح ردیف ۱۱:
- `wp_update_user` و `update_user_meta` داده‌ی ورودی را خودشان unslash می‌کنند.
- `wp_signon` (خط 55 در `wp-includes/user.php`) و ووکامرس رمز عبور را به همان شکل slash‌دار مقایسه می‌کنند.
- کد اصلی در این مورد درست بود (بخش ۴، مورد ۱).

**تغییر برای کاربر:** فرم تغییر رمز حالا یک فیلد «رمز فعلی» اضافه دارد.

**تست‌شده روی وردپرس واقعی:**
- ورود با رمزی که `'` و `\` دارد
- ذخیره‌ی نام `O'Brien \ test` و آدرسی که کوتیشن دارد، بدون بک‌اسلش اضافه
- رد شدن ایمیل نامعتبر
- رد شدن رمز فعلی اشتباه
- بی‌اثر بودن درخواست جعلی بدون nonce
- باقی ماندن لاگین بعد از تغییر رمز
- ورود با رمز جدیدی که `'` و `\` دارد
- خروج از حساب

**باگ‌های تأییدشده در کد اصلی:**
- یک POST جعلی بدون nonce و بدون رمز فعلی، رمز کاربر لاگین‌کرده را عوض کرد.
- رفتار ایمیل (ردیف ۷) و خروج از حساب بعد از تغییر رمز (ردیف ۶)، که در بالا آمد.

### ۲.۲ `post.php` (شورت‌کد `[current_post_article]` و کپچای نظرات)

| # | تغییر | خط |
|---|---|---|
| 1 | `if (!defined('ABSPATH')) exit;` اضافه شد. | 6 |
| 2 | دو بررسی «پشتیبان» حذف شدند: `fc_verify_captcha_insert` روی `wp_insert_comment` و `fc_verify_captcha_backup` روی `comment_post`. | (حذف‌شده) |
| 3 | یک بررسی واحد روی `preprocess_comment` باقی ماند (جزئیات زیر جدول). | 517 |
| 4 | کپچا حالا در **هر نوشته‌ی تکی** نمایش داده می‌شود (`is_singular('post')`)، نه فقط وقتی شورت‌کد اجرا شده باشد. پرچم `$GLOBALS['fc_captcha_active']` حذف شد. | 449، 555 |
| 5 | رنگ برچسب کپچا از `#000` به `rgba(255,255,255,0.85)` تغییر کرد. | 453 |
| 6 | کپچا با تأخیر بارگذاری می‌شود (جزئیات زیر جدول). | 647–673 |
| 7 | عمر کد کپچا از ۵ به ۲۰ دقیقه رسید. هر کد همچنان فقط یک بار قابل استفاده است. | 496 |
| 8 | تصویر شاخص: `srcset` و `sizes` و `fetchpriority="high"` و `decoding="async"` اضافه شد (جزئیات زیر جدول). | 31–44 |

**چرا بررسی‌های پشتیبان حذف شدند (ردیف ۲):**
- بررسی اول، کد کپچا را بعد از تأیید پاک می‌کرد (`delete_transient`).
- بعد این دو بررسی اجرا می‌شدند، کد را پیدا نمی‌کردند و نظر **معتبر** را حذف می‌کردند.
- تأییدشده روی کد اصلی: نظر مهمان با کپچای درست حذف شد و پیام «کد امنیتی نادرست. نظر بلافاصله حذف شد.» نمایش داده شد.
- مدیرها چون از بررسی معاف بودند، این مشکل را نمی‌دیدند.

**جزئیات بررسی واحد (ردیف ۳):**
- برای همه‌ی نظرهای نوشته‌های نوع `post` اجباری است، به جز pingback و trackback.
- کاربرانی که قابلیت `moderate_comments` دارند معاف‌اند (قبلاً فقط administrator معاف بود). **یعنی Editorها هم حالا معاف‌اند** (از روی کد).
- دیدگاه‌های محصولات ووکامرس تحت تأثیر نیستند (تست شد).
- قبلاً اگر فیلد `fc_captcha` ارسال نمی‌شد، بررسی کلاً رد می‌شد و ربات می‌توانست بدون کپچا نظر بگذارد. این روی کد اصلی تأیید شد.

**اثرهای جانبی ردیف ۳:**
- کاربر لاگین‌کرده‌ای که moderator نیست، اگر از REST API یا XML-RPC روی نوشته‌ها نظر بفرستد، رد می‌شود، چون کپچا ندارد (از روی کد).

**ردیف ۴:** تست شد که روی قالب معمولی کپچا نمایش داده می‌شود و نظر ثبت می‌شود.

**ردیف ۵:**
- برچسب مشکی قبلاً روی زمینه‌ی تیره‌ی شورت‌کد دیده نمی‌شد (روی کد اصلی تأیید شد).
- برچسب حالا سفید است. روی قالبی با زمینه‌ی روشن (یعنی نوشته‌ای که بدون شورت‌کد نمایش داده شود) این برچسب **تقریباً دیده نمی‌شود**، چون کادر کپچا زمینه‌ی تقریباً شفاف دارد.
- خود فیلد ورود کد زمینه‌ی تیره و متن سفید دارد و دیده می‌شود.
- در کد اصلی، کپچا روی چنین نوشته‌هایی اصلاً نمایش داده نمی‌شد.

**جزئیات بارگذاری با تأخیر (ردیف ۶):**
- درخواست AJAX `fc_generate_captcha` فقط وقتی ارسال می‌شود که:
  - کپچا تا ۳۰۰ پیکسل به دید کاربر برسد (`IntersectionObserver`)، یا
  - کاربر روی یکی از فیلدهای فرم کلیک کند (`focusin`).
- در مرورگر بدون `IntersectionObserver`، مثل قبل بلافاصله بارگذاری می‌شود.
- دلیل: قبلاً هر بازدید از هر نوشته یک درخواست admin-ajax غیرقابل‌کش و یک نوشتن در دیتابیس ایجاد می‌کرد.
- تست شد: در بازدید معمولی ۰ درخواست، بعد از رسیدن به فرم ۱ درخواست.

**دلیل ردیف ۷:** چون کد حالا وقتی کاربر به فرم می‌رسد ساخته می‌شود، نوشتن یک نظر طولانی نباید به منقضی شدن کد منجر شود.

**جزئیات تصویر شاخص (ردیف ۸):**
- `src` همچنان تصویر full است و فقط برای مرورگرهای بدون پشتیبانی srcset استفاده می‌شود.
- مقدار `sizes`:
  - روی دسکتاپ: `250px × نسبت`
  - در بقیه‌ی موارد: `360px × نسبت`
  - نسبت برابر `max(1, عرض/ارتفاع)` است، چون تصویر با `object-fit: cover` در قاب مربعی بریده می‌شود.
- اگر `wp_get_attachment_image_srcset` برای آن تصویر چیزی برنگرداند (یعنی کمتر از دو اندازه‌ی هم‌نسبت وجود داشته باشد)، srcset اضافه نمی‌شود و رفتار مثل قبل است.

**تغییر نکرده:** پاسخ AJAX کپچا (خط 499) خود کد را به صورت متن ساده برمی‌گرداند، پس ربات می‌تواند آن را بخواند (بخش ۵).

### ۲.۳ `digikala-product-archive.php` (شورت‌کد `[digikala_archive]`)

| # | تغییر | خط | دلیل |
|---|---|---|---|
| 1 | `dk_get_category_tree`: همه‌ی دسته‌ها یک بار در هر درخواست با یک `get_terms` خوانده می‌شوند (cache ایستا). قبلاً برای هر گره‌ی درخت یک `get_terms` جداگانه اجرا می‌شد. آرگومان `parent` حذف شد و دسته‌ها در PHP بر اساس والد گروه‌بندی می‌شوند. بقیه‌ی آرگومان‌ها و ترتیب (بر اساس نام، و ترتیب دستی برای زیردسته‌ها) تغییر نکرده است. | 16 | کاهش کوئری |
| 2 | `dk_sidebar_promo`: محصولات، termها و تصاویرشان با `_prime_post_caches` یکجا بارگذاری می‌شوند. این تابع از وردپرس 6.1 دیگر private نیست (طبق توضیحات خود وردپرس) و خود ووکامرس 10.1.2 هم در ۷ فایل از آن استفاده می‌کند. | 130 | کاهش کوئری |
| 3 | بعد از کوئری اولیه و در AJAX، `update_post_thumbnail_cache($query)` اضافه شد. این تابع عمومی وردپرس است. | 338، 1017 | تصاویر کارت‌ها را با دو کوئری کلی (posts و postmeta) بارگذاری می‌کند، به جای دو کوئری برای هر محصول. |
| 4 | `@font-face` برای Vazirmatn فقط برای فایل‌هایی نوشته می‌شود که در `{قالب فعال}/fonts/` وجود دارند (`file_exists`). اگر فایل‌ها موجود باشند، خروجی همان قبلی است. | 381 | جزئیات زیر جدول |
| 5 | در AJAX `dk_load_accordion_products`، بررسی nonce (`check_ajax_referer`) حذف شد. nonce هنوز ساخته می‌شود (خط 282) و جاوااسکریپت آن را می‌فرستد، ولی دیگر بررسی نمی‌شود. | 992 | جزئیات زیر جدول |
| 6 | `per_page` سقف ۱۰۰ دارد (پیش‌فرض ۱۲). | 1000 | endpoint عمومی است و مقدار بی‌سقف می‌توانست کوئری سنگین بسازد. |
| 7 | `uid` فقط `[a-zA-Z0-9_-]` می‌پذیرد و اگر خالی شود `dkacc` می‌شود. | 1006 | جزئیات زیر جدول |
| 8 | برای ورودی‌های `$_POST` مقدار پیش‌فرض `??` گذاشته شد. | 996–1003 | جلوگیری از هشدار PHP 8 |
| 9 | JS، `restoreAfterFailure`: اگر درخواست فیلتر خطا بدهد، محصولات قبلی و شماره‌ی صفحه برگردانده می‌شوند. قبلاً گرید پنهان می‌ماند. | 907 | |
| 10 | JS، `pendingReload`: اگر کاربر وسط یک درخواست فیلتر یا مرتب‌سازی را عوض کند، تغییرش بعد از پایان درخواست اعمال می‌شود. قبلاً بی‌صدا نادیده گرفته می‌شد (مشکل کد اصلی بود). | 847 | |

**جزئیات ردیف ۴ (فونت‌ها):**
- اگر فایل‌های فونت نباشند، مرورگر برای هر وزنِ فونتی که در صفحه استفاده شود دو درخواست ناموفق 404 می‌زند (woff2 و بعد woff). با سه وزن، حداکثر ۶ درخواست.
- **در Chromium تست شد:** این قاعده‌ی خراب، فونت سالمی را که جای دیگری تعریف شده باشد از کار **نمی‌اندازد**. مرورگر به فونت سالم برمی‌گردد. پس ضرر کد قبلی فقط همان درخواست‌های ناموفق بود (بخش ۴، مورد ۶).
- تست شد: بدون فایل، هیچ قاعده‌ای نوشته نمی‌شود. با وجود فایل‌های Regular.woff2، Bold.woff2 و Bold.woff، دقیقاً همان‌ها نوشته می‌شوند.
- **یک تفاوت رفتاری:** اگر فایل‌ها روی هاست نباشند، `local('Vazirmatn …')` هم دیگر نوشته نمی‌شود. یعنی کاربری که Vazirmatn را روی دستگاهش نصب کرده، اگر سایت فونت را جای دیگری تعریف نکرده باشد، به جای آن فونت جایگزین (Tahoma) را می‌بیند.

**جزئیات ردیف ۵ (nonce):**
- این endpoint فقط لیست عمومی محصولات منتشرشده را برمی‌گرداند.
- ورودی‌هایش چنین پردازش می‌شوند:
  - `root_cat`، `page`، `per_page` و `cats` به عدد صحیح تبدیل می‌شوند.
  - `min_price` و `max_price` به عدد اعشاری تبدیل می‌شوند.
  - `sort` فقط در یک `switch` با مقدار پیش‌فرض استفاده می‌شود.
  - `uid` با regex پاک‌سازی می‌شود.
  - `mode` با `sanitize_text_field` پاک‌سازی می‌شود و داخل `dk_build_query_args` اصلاً استفاده نمی‌شود.
- nonce کاربر مهمان را هر کسی با باز کردن صفحه به دست می‌آورد، پس امنیتی اضافه نمی‌کرد.
- در عوض، روی صفحه‌ی کش‌شده بعد از ۱۲ تا ۲۴ ساعت منقضی می‌شد و فیلترها و «نمایش بیشتر» از کار می‌افتادند.
- تأییدشده روی کد اصلی: nonce نامعتبر باعث پاسخ `403 -1` می‌شد.

**جزئیات ردیف ۷ (uid):**
- `uid` بدون escape داخل attributeهای HTML در `dk_render_product_card` چاپ می‌شود.
- تأییدشده روی کد اصلی: وقتی پاسخ داخل صفحه قرار گرفت، payload `" onmouseover=... x="` جاوااسکریپت اجرا کرد.
- **خطر عملی پایین است:** کد خود سایت هیچ‌وقت مقدار مخرب نمی‌فرستد. این اصلاح پیشگیرانه است.

### ۲.۴ `dk-single-product.php` (شورت‌کد `[dk_product_v2]`)

| # | تغییر | خط | دلیل |
|---|---|---|---|
| 1 | برای برند و برچسب، به جای `wp_get_post_terms` از `get_the_terms` استفاده می‌شود. | 32، 320 | `wp_get_post_terms` بدون cache است و هر بار کوئری می‌زند. ترتیب هر دو بر اساس نام است (از روی کد). |
| 2 | تصویر اصلی: کلاس `skip-lazy` و ویژگی‌های `fetchpriority="high"`، `loading="eager"` و `data-no-lazy="1"` اضافه شد. | 149 | این تصویر معمولاً بزرگ‌ترین المان صفحه (LCP) است. |
| 3 | در گالری، تصویری که فایلش حذف شده نادیده گرفته می‌شود. | 163 | جزئیات زیر جدول |
| 4 | مشخصات فنی: برای ویژگی‌های سراسری، به جای شناسه‌ها **نام termها** نمایش داده می‌شود (`wc_get_product_terms` با `fields=names`). برچسب و مقدار با `esc_html` چاپ می‌شوند. | 298 | تأییدشده روی کد اصلی: «رنگ: 283، 282» نمایش داده می‌شد. |
| 5 | نوار چسبان: برای محصول ناموجود نمایش داده نمی‌شود. برای محصول ساده، فرم قبلی باقی ماند. برای بقیه‌ی انواع (متغیر و گروهی و…) دکمه‌ای است که صفحه را به بخش خرید (`.dk2-buy`) اسکرول می‌کند. | 340 | تأییدشده روی کد اصلی: دکمه‌ی چسبان محصول متغیر، محصول را به سبد اضافه نمی‌کرد. |
| 6 | لایت‌باکس: امکان پیمایش تصاویر اضافه شد (جزئیات زیر جدول). | 422 | قبلاً فقط تصویر اول نمایش داده می‌شد (روی کد اصلی تأیید شد). |

**جزئیات ردیف ۲:** اینکه افزونه‌ی lazy-load سایت کلاس `skip-lazy` یا ویژگی `data-no-lazy` را می‌شناسد یا نه، بررسی نشده است.

**جزئیات ردیف ۳:**
- در کد اصلی، برای تصویر حذف‌شده یک لینک با `href` خالی ساخته می‌شد که مرورگر آن را به آدرس خود صفحه تبدیل می‌کند (روی کد اصلی تأیید شد).
- چون لایت‌باکس قبلی فقط تصویر اول را نشان می‌داد، این لینک در عمل اثری نداشت.
- این اصلاح لازم شد چون پیمایش لایت‌باکس (ردیف ۶) اضافه شد. بدون آن، یک اسلاید خراب نمایش داده می‌شد.
- تست شد: نسخه‌ی نهایی هیچ لینک خالی نمی‌سازد.

**جزئیات ردیف ۶ (لایت‌باکس):**
- دکمه‌های قبلی و بعدی، شمارنده‌ی «۲ / ۴» و بستن با Esc اضافه شد.
- کلید ← تصویر بعدی و کلید → تصویر قبلی را نشان می‌دهد (مطابق راست‌به‌چپ).
- کشیدن انگشت بیش از ۴۰ پیکسل تصویر را عوض می‌کند.
- با بستن لایت‌باکس، شنونده‌ی صفحه‌کلید حذف می‌شود.
- اگر CSS بیرونی به overlay موقعیت ندهد، `position: relative` گذاشته می‌شود تا دکمه‌ها سر جایشان بمانند.

**تغییر نکرده (بخش ۵):**
- نشان‌های ثابت «۱۰۰۰+ نفر…» (خطوط 208–210)
- متن «بازگشت بدون قید و شرط» (خط 257)
- اسکیمای غیرفعال با `FreeReturn` (خط 384)

### ۲.۵ `dk-wishlist.php`

| # | تغییر | خط | دلیل |
|---|---|---|---|
| 1 | `register_activation_hook` حذف شد. به جای آن، روی `init` اگر گزینه‌ی `dk_wishlist_db_version` برابر `'2'` نباشد، جدول با `dbDelta` ساخته یا به‌روز می‌شود و گزینه ذخیره می‌شود. | 47 | برای mu-pluginها هیچ‌وقت activation اجرا نمی‌شود. تأییدشده روی کد اصلی با دیتابیس تمیز: جدول ساخته نشد، ولی پاسخ `{"status":"added"}` برگشت. |
| 2 | به جدول دو ایندکس اضافه شد: `user_product (user_id, product_id)` و `session_product (session_id, product_id)`. | | کوئری‌های افزودن و بررسی روی این ستون‌ها جست‌وجو می‌کنند. |
| 3 | افزودن فقط برای شناسه‌هایی با نوع `product` یا `product_variation` پذیرفته می‌شود. در غیر این صورت پاسخ 400 است. | 83 | جلوگیری از پر شدن جدول با شناسه‌های بی‌ربط |
| 4 | اگر `insert` خطا بدهد، پاسخ `500 db_error` برمی‌گردد. قبلاً همیشه `added` برمی‌گشت. | 117 | |
| 5 | در بررسی علاقه‌مندی، مهمانی که کوکی ندارد بلافاصله `false` می‌گیرد و برایش کوکی ساخته نمی‌شود. | 140 | |

**نکته برای کارشناس:**
- اگر جدول روی سایت زنده از قبل وجود داشته باشد، `dbDelta` فقط دو ایندکس را اضافه می‌کند.
- این مسیر (جدول موجود) فقط روی SQLite تست شده، نه MySQL.
- کد جاوااسکریپت سمت سایت که این AJAXها را صدا می‌زند در این فایل‌ها نیست و آن را ندیده‌ام.

### ۲.۶ `cart-quantity-fix.php`

| # | تغییر | خط |
|---|---|---|
| 1 | `MutationObserver` روی `document.body` قبلاً با **هر** تغییر DOM بلافاصله `processQuantities()` را اجرا می‌کرد. حالا حداکثر یک بار در هر ۱۵۰ میلی‌ثانیه اجرا می‌کند (throttle، نه debounce). | 62 |

**چرا throttle و نه debounce:** المانی که دائم تغییر می‌کند (مثل تایمر) debounce را برای همیشه عقب می‌انداخت و پردازش هیچ‌وقت انجام نمی‌شد.

**چرا روی body:** ووکامرس بعد از به‌روزرسانی سبد، کل فرم را جایگزین می‌کند. در نسخه‌ی میانی من (`4213e8c`) observer فقط روی خود فرم بود و بعد از اولین به‌روزرسانی از کار می‌افتاد. این در `1899e1f` اصلاح شد.

**تست‌ها:**
1. **روی سبد واقعی ووکامرس:** حذف آیتم با AJAX و جایگزین شدن فرم. فیلدهای تعداد فرم جدید پردازش شدند.
2. **تست واحد در مرورگر:** جایگزین کردن فرم، در حالی که یک المان در صفحه هر ۳۰ میلی‌ثانیه تغییر می‌کرد. نسخه‌ی `4213e8c` رد شد و نسخه‌ی نهایی قبول شد.

**تغییر نکرده:** همین اسکریپت با `pointer-events:none` (خط 29) فیلد تعداد در سبد را **با موس غیرقابل‌ویرایش** می‌کند. این رفتار روی سبد واقعی تأیید شد (بخش ۵).

### ۲.۷ `bsma-performance.php` (فایل جدید)

این فایل فقط در سمت سایت (نه پنل مدیریت):
- `print_emoji_detection_script` را از `wp_head` برمی‌دارد.
- `wp_enqueue_emoji_styles` را از `wp_enqueue_scripts` برمی‌دارد (وردپرس 6.4 به بعد).
- `print_emoji_styles` را از `wp_print_styles` برمی‌دارد (قبل از 6.4).
- لینک dns-prefetch مربوط به `s.w.org/images/core/emoji` را از `wp_resource_hints` حذف می‌کند.

دلیل: مرورگرهای امروزی ایموجی را خودشان نمایش می‌دهند.

تست شد:
- در صفحه‌ی سایت نه اسکریپت ایموجی هست، نه prefetch به s.w.org.
- در `/wp-admin/` اسکریپت ایموجی همچنان وجود دارد.

---

## ۳. اندازه‌گیری‌ها

همه‌ی اعداد مربوط به محیط تست بخش ۱ هستند، نه سایت زنده.

### کوئری و زمان اجرای شورت‌کد

قبل یعنی `1899e1f` و بعد یعنی `951b2f4`. زمان‌ها میانه‌ی ۳ اجرا هستند.

| صفحه | کوئری قبل | کوئری بعد | زمان قبل (ms) | زمان بعد (ms) |
|---|---|---|---|---|
| آرشیو دسته‌ی 279 (با کارت تبلیغاتی) | 36 | 18 | 47.2 | 33.7 |
| آرشیو همه‌ی محصولات | 38 | 18 | 38.0 | 24.3 |
| صفحه‌ی محصول | 15 | 13 | 15.4 | 12.0 |
| نوشته‌ی وبلاگ | 4 | 4 | 5.0 | 5.2 |

**نکته‌ها:**
- در سایت واقعی، که هر محصول تصویر جداگانه‌ی خودش را دارد، صرفه‌جویی تصاویر در آرشیو با تعداد محصولات صفحه بیشتر می‌شود. در داده‌ی تست، ۱۲ تصویر بین محصولات مشترک بود.
- حجم HTML صفحه‌ها کم شد:

  | صفحه | قبل (بایت) | بعد (بایت) |
  |---|---|---|
  | آرشیو دسته | 98,058 | 93,077 |
  | آرشیو همه‌ی محصولات | 89,784 | 84,803 |
  | محصول | 54,164 | 50,520 |
  | نوشته | 51,504 | 49,486 |

  عامل اصلی این کاهش حذف اسکریپت و استایل ایموجی است. در صفحه‌های آرشیو، بخشی هم به خاطر حذف `@font-face` است، چون قالب تست فایل Vazirmatn ندارد. اگر روی سایت فایل‌ها موجود باشند، آن بخش باقی می‌ماند.

### حجم تصویر شاخص وبلاگ

در همه‌ی ۸ حالت زیر، ابعاد پیکسلی فایلی که مرورگر انتخاب کرد، برای پر کردن قاب در آن تراکم پیکسلی کافی بود (با محاسبه، نه بررسی چشمی). «قبل» در همه‌ی حالت‌ها فایل full بود.

| حالت | قبل | بعد |
|---|---|---|
| افقی، موبایل 3x | 359KB | 195KB |
| افقی، موبایل 2x | 359KB | 138KB |
| افقی، دسکتاپ 1x | 359KB | 44KB |
| افقی، دسکتاپ 2x | 359KB | 84KB |
| عمودی، موبایل 3x | 201KB | 139KB |
| عمودی، موبایل 2x | 201KB | 83KB |
| عمودی، دسکتاپ 1x | 201KB | 64KB |
| عمودی، دسکتاپ 2x | 201KB | 64KB |

### هزینه‌ی بارگذاری فایل‌ها

- هزینه‌ی بارگذاری هر ۱۰ فایل mu-plugins در هر درخواست:
  - با OPcache: حدود 0.8 میلی‌ثانیه
  - بدون OPcache: حدود 4.6 میلی‌ثانیه
- این اندازه‌گیری با توابع شبیه‌سازی‌شده‌ی وردپرس انجام شد.
- به همین دلیل `ثلاث.php` برای سرعت تغییر داده نشد (سهمش 0.3 تا 2.3 میلی‌ثانیه است).

---

## ۴. اشتباه‌های من در طول کار و اصلاح آن‌ها

1. **unslash کردن فیلدهای فرم حساب (در `4213e8c`):**
   - روی رمز، ایمیل، نام و آدرس‌ها `wp_unslash` گذاشته بودم و ادعا کرده بودم کد اصلی «بک‌اسلش اضافه ذخیره می‌کند».
   - **این ادعا غلط بود.**
   - تغییر من باعث می‌شد:
     - رمز جدیدی که `'` یا `\` دارد دیگر برای ورود کار نکند
     - بک‌اسلشی که کاربر عمداً تایپ کرده حذف شود
   - روی وردپرس واقعی کشف شد و در `951b2f4` برگردانده شد.
   - روی کد اصلی هم تأیید شد که ورود با چنین رمزی کار می‌کرد و نام `O'Brien` درست ذخیره می‌شد.
2. **observer سبد خرید (در `4213e8c`):** فقط روی فرم نصب شده بود و بعد از اولین به‌روزرسانی سبد از کار می‌افتاد. در `1899e1f` اصلاح شد.
3. **کپچا (در `4213e8c`):** کپچا را برای همه‌ی نظرهای نوشته‌ها اجباری کرده بودم، در حالی که فقط در شورت‌کد نمایش داده می‌شد. در `1899e1f` نمایش آن به همه‌ی نوشته‌ها گسترش یافت.
4. **شدت XSS:** مشکل uid در آرشیو را آسیب‌پذیری جدی معرفی کرده بودم. واقعی است ولی خطر عملی‌اش پایین است (بخش ۲.۳، ردیف ۷).
5. **فایل `یع جع.php`:** پیشنهاد حذفش را داده بودم. اشتباه بود: شورت‌کد `[translate_dropdown]` در همین فایل به `https://bsma.ir/صناديق-إطفاء-الحريق/` لینک می‌دهد، پس احتمالاً در حال استفاده است.
6. **فونت‌ها (در `951b2f4`):** در گفتگو و در توضیح داخل کد نوشته بودم که قاعده‌ی `@font-face` خراب، فونت سالم Vazirmatn را از کار می‌اندازد.
   - **این ادعا غلط بود.** در Chromium تست شد و مرورگر به فونت سالم برمی‌گردد.
   - ضرر واقعی فقط درخواست‌های ناموفق 404 است.
   - خود اصلاح درست است. فقط توضیح داخل کد در commit آخر اصلاح شد.

---

## ۵. موارد باز (تغییری نداده‌ام، تصمیم با صاحب سایت یا کارشناس است)

1. ~~**کپچا ضعیف است**~~ — در بخش ۷ انجام شد.
2. **سه دکمه‌ی پنل `ثلاث.php` خطای PHP می‌دهند:** در این فایل سه اکشن AJAX به متدهایی وصل شده‌اند که اصلاً تعریف نشده‌اند:
   - `ajax_fix_issue`
   - `ajax_save_public`
   - `ajax_regen_token`

   - روی وردپرس تست شد: هر سه اکشن (`thalath_fix_issue`، `thalath_save_public`، `thalath_regen_token`) برای ادمین لاگین‌کرده خطای 500 می‌دهند.
   - خطای ثبت‌شده: `Uncaught TypeError: call_user_func_array(): Argument #1 ($callback) must be a valid callback`.
   - این اکشن‌ها فقط برای کاربران لاگین‌کرده ثبت شده‌اند (`wp_ajax_`)، پس روی بازدیدکننده‌ها اثری ندارند.
3. ~~**آمار نمایشی ثابت**~~ — در بخش ۷ انجام شد.
4. **تناقض در شرایط مرجوعی:**
   - متن نشان محصول (`dk-single-product.php`، خط 257): «بازگشت بدون قید و شرط»
   - `bsma-product-schema-tweaks.php`، خط 26: هزینه‌ی ارسال مرجوعی با مشتری است
   - اسکیمای غیرفعال `dk-single-product.php`، خط 384: `FreeReturn`
5. **فیلد تعداد در سبد با موس قابل ویرایش نیست:** `cart-quantity-fix.php`، خط 29 (`pointer-events:none`). معلوم نیست عمدی است یا نه.
6. **همه‌ی سفارش‌ها یک‌جا بارگذاری می‌شوند:** `fire-account-widget.php`، خط 225، `wc_get_orders([... 'limit' => -1])`. برای مشتری با سفارش‌های زیاد کند است.
7. **فایل‌های فونت:** باید بررسی شود فایل‌های `wp-content/themes/{قالب فعال}/fonts/Vazirmatn-*.woff2` روی هاست وجود دارند یا نه (بخش ۲.۳، ردیف ۴).
8. **سایت زنده اندازه‌گیری نشده است.** بزرگ‌ترین بهبودهای سرعت بیرون از این کدها هستند:
   - کش صفحه
   - بهینه‌سازی تصاویر
   - کاهش افزونه‌ها
9. **کش صفحه‌ی حساب کاربری:** اگر صفحه‌ی حساب برای کاربران لاگین‌کرده کش شود، nonce فرم‌ها کهنه می‌شود و پیام «اعتبار فرم تمام شده است» نمایش داده می‌شود.

---

## ۶. نصب روی هاست

1. از فایل‌های فعلی `wp-content/mu-plugins` نسخه‌ی پشتیبان بگیرید.
2. ۸ فایل را جایگزین یا اضافه کنید:
   - `bsma-performance.php` (جدید)
   - `bsma-lscwp-no-remote-sizes.php` (جدید، بخش ۷.۱)
   - `cart-quantity-fix.php`
   - `digikala-product-archive.php`
   - `dk-single-product.php`
   - `dk-wishlist.php`
   - `fire-account-widget.php`
   - `post.php`
3. این موارد را تست کنید:
   - ثبت نظر وقتی از حساب خارج شده‌اید
   - تغییر رمز در صفحه‌ی حساب (حالا رمز فعلی لازم است)
   - فیلتر، مرتب‌سازی و «نمایش بیشتر» در صفحه‌ی دسته‌بندی
   - تغییر در سبد خرید
   - گالری و نوار چسبان محصول ساده و متغیر
   - دکمه‌ی علاقه‌مندی
4. زیپ موقت روی هاست را پاک کنید: `/home/bsma/tmp/mu-plugins-php.zip`

---

## ۷. تغییرات بعدی (بعد از commit `9c9426a`)

شماره‌ی خط‌های این بخش مربوط به نسخه‌ی نهایی شاخه هستند.

### ۷.۱ `bsma-lscwp-no-remote-sizes.php` (فایل جدید): کندی ۶۰ ثانیه‌ای دسته‌ی fire-box

**علت:**
- گزینه‌ی «Add Missing Sizes» در LiteSpeed Cache 7.9.1 (`src/media.cls.php`، تابع `_detect_dimensions`، خطوط 1145 تا 1168) برای هر `<img>` که `width` یا `height` ندارد و فایلش روی همین سرور نیست، `getimagesize()` را روی آدرس اینترنتی اجرا می‌کند.
- اگر هاست تصویر جواب ندهد، PHP به اندازه‌ی `default_socket_timeout` (پیش‌فرض ۶۰ ثانیه) منتظر می‌ماند.
- اگر Guest Optimization روشن باشد، این قابلیت برای مهمان‌ها همیشه روشن است (`src/vary.cls.php`، خط 222).

**اصلاح:** `add_filter('litespeed_media_ignore_remote_missing_sizes', '__return_true');`. این فیلتر رسمی خود LiteSpeed است و داخل همان تابع بررسی می‌شود.

**تست:** با LiteSpeed 7.9.1 روی وردپرس تست، و یک سرور محلی که اتصال را می‌پذیرد ولی پاسخ نمی‌دهد:
- بدون اصلاح: **۶۰٫۹ ثانیه** و HTTP 200
- با اصلاح: **۰٫۷ ثانیه**
- تصویر محلی بدون ابعاد همچنان `width="600" height="338"` گرفت.

**روی سایت زنده تأیید نشده است.** راه تأیید: بعد از آپلود فایل، یک درخواست بدون کش به `/product-category/fire-box/` بزنید.

### ۷.۲ `dk-single-product.php`: آمار واقعی به جای اعداد ثابت

**تغییرات:**
- تابع `dk2_social_proof_badges` (خط 16) و تابع `dk2_fa_number` (خط 61، تبدیل عدد به ارقام فارسی) اضافه شدند.
- محل نمایش: خط 265.
- **علاقه‌مندی:** تعداد ردیف‌های این محصول در جدول `dk_wishlist` («۳ نفر در لیست علاقه‌مندی»). این عدد فقط وقتی خوانده می‌شود که گزینه‌ی `dk_wishlist_db_version` وجود داشته باشد، یعنی جدول ساخته شده باشد.
- **فروش:** تعداد سفارش‌های **پرداخت‌شده** (`wc-processing` و `wc-completed`) در ۷ روز گذشته، از جدول‌های Analytics ووکامرس (`wc_order_product_lookup` و `wc_order_stats`). سفارش‌های مرجوعی (`parent_id > 0`) حساب نمی‌شوند. متن: «۲ سفارش در هفته گذشته». تعداد **سفارش** شمرده می‌شود، نه تعداد قطعه.
- **بازدید:** نشان «۱۰۰+ بازدید» **حذف شد**، چون سایت آمار بازدید ثبت نمی‌کند.
- **اگر عدد صفر باشد:** آن نشان نمایش داده نمی‌شود. اگر هیچ نشانی نباشد، خود جعبه‌ی `.dk2-badges` هم نمایش داده نمی‌شود.
- **Cache:** نتیجه برای هر محصول یک ساعت در transient `dk2_badges_{id}` ذخیره می‌شود. کش صفحه‌ی LiteSpeed هم ممکن است عدد را تا پاک شدن کش ثابت نگه دارد.

**محدودیت:** اگر Analytics ووکامرس غیرفعال باشد یا داده‌های قدیمی وارد نشده باشند (WooCommerce ← Settings ← Advanced ← Analytics)، نشان فروش نمایش داده نمی‌شود. این حالت را روی سایت زنده بررسی نکرده‌ام.

**تست:** برای یک محصول این داده‌ها ساخته شد: ۳ علاقه‌مندی، و این سفارش‌ها:
- پرداخت‌شده در همین هفته: ۲ سفارش (یکی processing، یکی completed)
- pending: ۱ سفارش
- refunded: ۱ سفارش
- completed، ۱۰ روز پیش: ۱ سفارش

نتیجه:
- نمایش داده شد: «۳ نفر» و «۲ سفارش».
- نشان بازدید و اعداد ثابت قدیمی دیگر نبودند.
- محصول بدون داده هیچ جعبه‌ای نداشت.

### ۷.۳ `post.php`: کپچای ساخته‌شده روی سرور

**ساخت تصویر:**
- تابع جدید `fc_captcha_image` (خط 508) تصویر PNG به ابعاد 200×60 را با GD روی سرور می‌سازد و به صورت data URI برمی‌گرداند.
- حروف با فونت داخلی GD کشیده می‌شوند، سه برابر بزرگ می‌شوند و هرکدام تا ±۲۵ درجه چرخش تصادفی می‌گیرند.
- نقطه‌های پراکنده و سه خط موجی روی حروف اضافه می‌شود.
- طرح رنگ همان کپچای قبلی است.

**پاسخ AJAX:**
- پاسخ `fc_generate_captcha` (خط 500) حالا `{id, image}` است و **دیگر `code` ندارد**.
- فقط اگر GD روی سرور نباشد، مثل قبل `code` فرستاده می‌شود و مرورگر آن را می‌کشد.

**یک بار تلاش برای هر کد:** با پاسخ اشتباه هم کد پاک می‌شود (خط 607)، تا نتوان یک تصویر را با حدس‌زدن پشت سر هم شکست.

**جاوااسکریپت:**
- تصویر سرور روی همان canvas قبلی کشیده می‌شود (خط 653)، پس ظاهر و CSS تغییری نکرده است.
- اگر کاربر از صفحه‌ی خطا «بازگشت» بزند و مرورگر صفحه را از cache بیاورد، کپچای تازه گرفته می‌شود (رویداد `pageshow`، خط 763).

**تست:**
- ۶ نمونه تصویر با چشم بررسی شد و همه خوانا بودند.
- در پاسخ AJAX و در صفحه، جواب وجود ندارد.
- تصویر روی canvas کشیده می‌شود.
- با پاسخ اشتباه 403 برمی‌گردد و بعد از آن جواب درست با همان کد هم 403 می‌گیرد.
- بعد از «تصویر جدید»، جواب درست نظر را ثبت می‌کند.
- همه‌ی ۵۴ تست سرتاسری و ۶ تست تکمیلی دوباره قبول شدند و خطای PHP ثبت نشد.

**محدودیت‌ها:**
- این کپچا از قبلی خیلی قوی‌تر است، ولی ربات مجهز به OCR پیشرفته ممکن است همچنان آن را بخواند.
- وجود GD روی هاست بررسی نشده است. وردپرس معمولاً برای ساخت اندازه‌های تصویر از GD یا Imagick استفاده می‌کند. اگر GD نباشد، کپچا به حالت قبلی برمی‌گردد و کار می‌کند.
